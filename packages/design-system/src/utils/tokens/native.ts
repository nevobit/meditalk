import { jsTokens } from "./index";

export const rnTokens = {
    spacing: jsTokens.space,
    radius: jsTokens.radius,
    zIndex: jsTokens.zIndex,
    fontFamily: { sans: jsTokens.font.sans, mono: jsTokens.font.mono },
    fontSize: jsTokens.font.size,
    color: {
        accent: "#1868e8",
        fg: "#111827",
        bg: "#ffffff",
        success: "#16a34a",
        warning: "#d97706",
        danger: "#dc2626"
    }
};
