---
title: 'Tokens → Text'
description: 'Tokens → Text tokens'
---

import { TokenTable } from '../../../src/components/tokens/TokenTable';
/* extend with weights/line-heights here */

# Tokens → Text

> Extend with weights/line-heights by exposing them in `src/tokens/` and rendering with `<TokenTable kind="text" />`.
